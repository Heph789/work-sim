{
  "//": "Cross-boundary types and the LLMClient interface. Imported by apps/api at compile time via the @work-sim/shared tsconfig path alias. The web app will import the same package once scaffolded.",
  "name": "@work-sim/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "// DEPENDENCY zod": "Used both for HTTP request validation in apps/api and for re-validating LLM structured responses (defense in depth).",
    "zod": "^3.23.0"
  }
}
