// Scenario: empty. No runs at all — exercises the "No runs yet" empty state
// on the list page.

import type { ScenarioBuilder } from '../types.js';

export const empty: ScenarioBuilder = () => [];
